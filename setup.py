from setuptools import setup

setup(
    name='chatai',
    version='1.0.2',
    description='A python library for AI',
    author='Capsize LLC',
    install_requires=[
        "aihandler==1.8.10",
    ]
)
